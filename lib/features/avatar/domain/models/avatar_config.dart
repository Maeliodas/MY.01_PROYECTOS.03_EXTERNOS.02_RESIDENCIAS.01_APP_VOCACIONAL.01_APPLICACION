class AvatarConfig {
  final String id, label;
  const AvatarConfig(this.id, this.label);
}

const avatarOptions = [
  AvatarConfig('A', 'Avatar A'),
  AvatarConfig('B', 'Avatar B'),
  AvatarConfig('C', 'Avatar C'),
  AvatarConfig('D', 'Avatar D'),
];
